/**
 * handles fetching the language and localized text
 * @param textKey the wanted language key
 * @returns the localized text as string
 */
async function GetLocalizedText(textKey:string):string {
    var json = import("../assets/lang.json")
    return ""
}